docs/TERRAIN3D_CUSTOM_BUILD_GUIDE.md
and docs/TERRAIN3D_BUILD_FROM_SOURCE.md should be merged (same topic)

---

Triple check that horizon maps are actually loaded and working.
Need test scene

---

clouds (weather toggle in Godotwind) disappar when Sky3d is activated (Sky) in Godotwind scene
for example, big clouds in the Foggy preset. or stormy dark cloudcover in the rain preset. They disappear entirely when triggering Sky.

--
The clouds that are hidden by the Sky plugin have a weird behavior : when we move the camera, it's as if they are 1 frame behind.

---

Lots of bugs of time stopping. Can't edit the clouds on the fly. Sliders of fog, clouds, speed = do nothing.

--

Need to refactor the fog. Need a proper typology of fogs. Ground fog ? Atmosphere fog ? Rolling fog ?

Need to look into Morrowind's shaders

or : https://github.com/zesterer/openmw-shaders

Ideally we should allow such shaders to work.

--
God rays should be more visible


--
Ocean parameters are not connected to the weather

Should be unified = weather influences wind speed, which influences the waves height and aspect but also influences cloud speed etc

--
Rain and snow are way too shy, we need more particles. 
Check a wetworld-style wetness shader for reference

---
No rain occlusion
We need rain occlusion (look at how Openmw is doing it)


---

